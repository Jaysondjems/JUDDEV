[build]
buildCommand = "cd JUDDEV-backend && npm install --omit=dev"

[deploy]
startCommand = "node JUDDEV-backend/server.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
